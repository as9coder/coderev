# Copy to .env — only five values (MODEL has a default).
# Save your GitHub App PEM next to this file as github-app.pem (see .gitignore).

APP_ID=
PRIVATE_KEY_FILE=./github-app.pem
WEBHOOK_SECRET=
OPENROUTER_KEY=
ALLOWED_USER=
